#!/usr/bin/env bash

set -euo pipefail
IFS=$'\n\t'

filename="$1"
original="$2"
directory="$3"

base="$(basename "$filename")"

cat "$filename" | xargs -I {} cat {} > "$directory/$original-$base.ncr.gz"
