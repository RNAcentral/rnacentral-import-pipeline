params {
  common_container = "--contain --workdir $baseDir/work/tmp --bind $baseDir --bind /scratch"
}

process {
  executor = 'slurm'
  maxForks = 2000
}

executor {
  $slurm {
    queueSize = 10000
    submitRateLimit = '1sec'
  }
}

// vim: ft=groovy
