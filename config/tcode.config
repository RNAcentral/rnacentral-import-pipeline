params {
  tcode {
    run = true
    chunk_size = 1000
    window = 200
    query = "files/tcode/query.sql"
    load = true
    min_len = 200
    db_chunk_size = 100000
    max_forks = 3500
  }
}

process {
  maxForks = params.tcode.max_forks
}
